# Base URL of the backend API, e.g. https://api.example.com
# When unset, the app runs in mock mode (no real network requests).
VITE_API_BASE_URL=

# Set to "true" to force mock mode even when VITE_API_BASE_URL is provided.
VITE_MOCK_API=false
